# CODEX_START_PROMPT.md

Use this prompt to start a new Codex chat after moving the project into a proper repository/project workflow.

```text
Привет. Это новый чат по существующему проекту.

Сначала ничего не меняй в коде.

Проект: `friskiesman-hub/Presentation_demo`.
Локально он находится здесь:

/Users/alexanderchernomorets/Documents/Codex/2026-05-22/web-5-bork-senior-web-designer/Presentation_demo

Задача первого шага — восстановить контекст проекта без потери качества и без случайных изменений.

Пожалуйста:

1. Открой и внимательно прочитай:
   - `PROJECT_CONTEXT.md`
   - `AGENTS.md`
   - `QA_CHECKLIST.md`
   - `DEPLOY.md`

2. Проверь текущее состояние проекта:
   - `git status --short`
   - `git branch --show-current`
   - `git log -1 --oneline`
   - `git remote -v`

3. Кратко опиши, что ты понял:
   - что это за проект;
   - какая текущая стабильная версия;
   - сколько должно быть слайдов;
   - какие файлы являются ключевыми;
   - какие зоны наиболее рискованные;
   - какие ассеты нельзя случайно считать частью стабильной версии;
   - почему исходный архивный промпт не является актуальной технической спецификацией.

4. Сравни фактическую структуру репозитория с `PROJECT_CONTEXT.md`.

5. Ничего не редактируй, не создавай файлы и не запускай рефакторинг, пока я явно не попрошу.

Важно:
- проект не нужно переписывать на React / Next.js / TypeScript;
- не используй старую метафору “пять залов бутика” как актуальную спецификацию;
- текущая формула проекта: премиальная web-презентация бизнес-результатов BORK Internet Boutique Q2 2026 в эстетике флагманского бутика;
- не используй `git add .`;
- стабильность, читаемость, Safari/iPhone и аккуратный motion важнее декоративного усложнения.
```
